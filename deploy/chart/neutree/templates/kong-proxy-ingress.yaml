{{- if .Values.kong.ingress.enabled -}}
{{- $ing := .Values.kong.ingress -}}
{{- if not $ing.host }}{{ fail "kong.ingress.enabled is true but kong.ingress.host is empty" }}{{ end }}
{{- $host := tpl (toString $ing.host) . -}}
{{- if not $host }}{{ fail "kong.ingress.enabled is true but kong.ingress.host renders empty" }}{{ end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "neutree.fullname" . }}-kong-proxy
  labels:
    {{- include "neutree.labels" . | nindent 4 }}
  {{- with $ing.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with $ing.className }}
  ingressClassName: {{ tpl (toString .) $ }}
  {{- end }}
  {{- with $ing.tls }}
  tls:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: {{ $ing.path | default "/" | quote }}
            pathType: {{ $ing.pathType | default "Prefix" | quote }}
            backend:
              service:
                name: {{ include "neutree.fullname" . }}-kong-proxy
                port:
                  number: 80
{{- end -}}
