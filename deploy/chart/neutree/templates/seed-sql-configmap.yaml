kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "neutree.fullname" . }}-seed-sql
  labels:
    {{- include "neutree.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "db/seed/*").AsConfig | indent 2 }}