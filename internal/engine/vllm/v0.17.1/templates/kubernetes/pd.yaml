apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .EndpointName }}
  namespace: {{ .Namespace }}
  labels:
    engine: {{ .EngineName }}
    engine_version: {{ .EngineVersion }}
    routing_logic: {{ .RoutingLogic }}
    app: inference
    neutree.io/component: pd-collocated
spec:
  replicas: {{ .Replicas }}
  progressDeadlineSeconds: 1200
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  selector:
    matchLabels:
      cluster: {{ .ClusterName }}
      workspace: {{ .Workspace }}
      endpoint: {{ .EndpointName }}
      app: inference
  template:
    metadata:
      labels:
        engine: {{ .EngineName }}
        engine_version: {{ .EngineVersion }}
        cluster: {{ .ClusterName }}
        workspace: {{ .Workspace }}
        endpoint: {{ .EndpointName }}
        routing_logic: {{ .RoutingLogic }}
        app: inference
        neutree.io/component: pd-collocated
        neutree.io/leader: "true"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: endpoint
                      operator: In
                      values:
                        - {{ .EndpointName }}
                topologyKey: "kubernetes.io/hostname"
      {{- if .NodeSelector }}
      nodeSelector:
        {{- range $key, $value := .NodeSelector }}
        {{ $key }}: {{ $value }}
        {{- end }}
      {{- end }}
      {{- if .ImagePullSecret }}
      imagePullSecrets:
        - name: {{ .ImagePullSecret }}
      {{- end }}

      {{- if .Volumes }}
      volumes:
{{ .Volumes | toYaml | indent 6 }}
      {{- end }}
      initContainers:
        - name: model-downloader
          image: {{ .ImagePrefix }}/neutree/neutree-runtime:{{ .NeutreeVersion }}
          command:
            - bash
            - -c
          args:
            - >-
              python3 -m neutree.downloader
              --name="{{ .ModelArgs.name }}"
              --registry_type="{{ .ModelArgs.registry_type }}"
              --registry_path="{{ .ModelArgs.registry_path }}"
              --path="{{ .ModelArgs.path }}"
              --version="{{ .ModelArgs.version }}"
              --file="{{ .ModelArgs.file }}"
              --task="{{ .ModelArgs.task }}"
          env:
           {{ range $key, $value := .Env }}
           - name: {{ $key }}
             value: "{{ $value }}"
           {{ end }}
          {{- if .VolumeMounts }}
          volumeMounts:
{{ .VolumeMounts | toYaml | indent 10 }}
          {{- end }}

      containers:
        - name: pd-router-sidecar
          image: {{ .PD.Sidecar.Image }}
          env:
            - name: NEUTREE_PD_CONFIG
              value: {{ .PD.ConfigJSON | quote }}
            - name: NEUTREE_PD_SIDECAR_PORT
              value: "{{ .PD.Sidecar.Port }}"
            {{- range $key, $value := .Env }}
            - name: {{ $key }}
              value: "{{ $value }}"
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .PD.Sidecar.Port }}
          startupProbe:
            httpGet:
              path: {{ .PD.Sidecar.HealthPath }}
              port: {{ .PD.Sidecar.Port }}
            initialDelaySeconds: 5
            timeoutSeconds: 5
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 120
          readinessProbe:
            httpGet:
              path: {{ .PD.Sidecar.HealthPath }}
              port: {{ .PD.Sidecar.Port }}
            initialDelaySeconds: 5
            timeoutSeconds: 5
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
        {{- range .PD.Containers }}
        - name: {{ .Name }}
          image: {{ $.ImagePrefix }}/{{ $.ImageRepo }}:{{ $.ImageTag }}
          command:
          - vllm
          - serve
          - {{ $.ModelArgs.path }}
          - --host
          - "0.0.0.0"
          - "--port"
          - "{{ .Port }}"
          - --served-model-name
          - {{ $.ModelArgs.serve_name }}
          {{- $engineArgs := .EngineArgs }}
          {{- if and (not (hasKey $engineArgs "kv-transfer-config")) (not (hasKey $engineArgs "kv_transfer_config")) }}
          {{- $kvConnector := "NixlConnector" }}
          {{- if eq (lower .KVTransferConnector) "mooncake" }}
          {{- $kvConnector = "MooncakeConnector" }}
          {{- end }}
          {{- $kvRole := "kv_producer" }}
          {{- if eq .Role "decode" }}
          {{- $kvRole = "kv_consumer" }}
          {{- end }}
          {{- $kvBufferSize := 5000000000 }}
          {{- if hasKey .KVTransferExtra "buffer_size" }}
          {{- $kvBufferSize = get .KVTransferExtra "buffer_size" }}
          {{- end }}
          {{- $kvConfig := dict "kv_connector" $kvConnector "kv_role" $kvRole "kv_buffer_size" $kvBufferSize }}
          {{- if hasKey .KVTransferExtra "kv_buffer_device" }}
          {{- $_ := set $kvConfig "kv_buffer_device" (get .KVTransferExtra "kv_buffer_device") }}
          {{- end }}
          - --kv-transfer-config
          - {{ $kvConfig | toJson | quote }}
          {{- end }}
          {{- if .EngineArgs }}
          {{- range $key, $value := .EngineArgs }}
          - --{{ $key }}
      {{- if ne (printf "%v" $value) "true"}}
          - "{{ $value }}"
      {{- end }}
          {{- end }}
          {{- end }}
          {{- if .Resources }}
          resources:
            limits:
              {{- range $key, $value := .Resources }}
              {{ $key }}: {{ $value }}
              {{- end }}
            requests:
              {{- range $key, $value := .Resources }}
              {{ $key }}: {{ $value }}
              {{- end }}
          {{- end }}
          env:
           {{ range $key, $value := .Env }}
           - name: {{ $key }}
             value: "{{ $value }}"
           {{ end }}
          ports:
            - containerPort: {{ .Port }}
            {{- if gt .SideChannelPort 0 }}
            - containerPort: {{ .SideChannelPort }}
            {{- end }}
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Port }}
            initialDelaySeconds: 5
            timeoutSeconds: 5
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 120
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Port }}
            initialDelaySeconds: 5
            timeoutSeconds: 5
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          {{- if $.VolumeMounts }}
          volumeMounts:
{{ $.VolumeMounts | toYaml | indent 10 }}
          {{- end }}
        {{- end }}
